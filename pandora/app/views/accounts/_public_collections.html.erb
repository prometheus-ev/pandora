<table class="list resourceful-list">
  <%= render :partial => 'shared/resourceful/row', :collection => @public_collections.sort_by(&:updated_at).reverse, :locals => { :name => 'collection', :public_view => true, :cycle_name => 'public' } %>
</table>
