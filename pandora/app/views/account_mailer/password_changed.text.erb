Dear <%= @name %>,

the password for your prometheus account has been changed.
You can log in with your new credentials on our homepage:

<<%= @home_url %>>

The change has been executed <% unless @is_self -%>by

  <%= @originator %>

<% end -%>from the IP address <%= @ip %>.

If you <%= @is_self ? "didn't initiate a password change for your account" : 'think this password change is spurious' %>,
please contact the prometheus office immediately!

Kind regards,
your prometheus team

<%= render :partial => 'signature' -%>
