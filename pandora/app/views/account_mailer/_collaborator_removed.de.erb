Sie wurden als Mitarbeiter der folgenden <%= @type.t %> entfernt:

<%= @object %>
<<%= @object_url %>>

Dies bedeutet, dass Sie die <%= @type.t %> nicht länger bearbeiten
können, es sei denn, sie ist öffentlich schreibbar.

Sie können den Besitzer dieser <%= @type.t %>, <%= @owner %>,
erreichen, indem Sie auf diese E-Mail antworten oder ihm/ihr von
der Profilseite aus eine Nachricht senden:
<<%= @profile_url %>>
