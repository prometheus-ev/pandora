<%= submenu_link_to('List'.t, action: 'index') unless current_user && current_user.ipuser? %>
<%= submenu_link_to('Licensed'.t, action: 'licensed') unless current_user && current_user.ipuser? %>

<% restrict_to :superadmin, :admin do -%>
  <%= link_to create_button('Create a new Institution'.t, :plus), action: 'new' %>
<% end -%>