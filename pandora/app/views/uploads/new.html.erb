<%= render_sections %w[header] %>

<%= error_messages_for :upload %>

<!-- REWRITE: we need to specify the object and the param so that the url can be
calculated automatically -->
<%= form_for @upload, as: 'upload', :html => { :multipart => true, :onsubmit => 'return check_input();', :class => 'edit-form' } do |f| -%>
  <%= render_section Upload, 'object_obligatory!', f: f %>
  <p><br/></p>
<% end -%>
